#include <bits/stdc++.h>
using namespace std;
int main()
{
    // (a^b) % m = (a ^ (b % etf(m))) % m;
    // ETF of m = m * sigma(1 - 1/pf);  pf ----> prime factors
    // ETF of m when m is prime is m-1.

    // arr[]= {1, 2, 3, 8, 9};
    // To form a number using this array
    // ans = 0;
    // for(int i=0; i < arr.size(); i++)
    // {
    //    ans = (ans*10) + arr[i];
    // }
    // return ans;
return 0;
}